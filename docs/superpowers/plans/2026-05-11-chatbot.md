# Chatbot widget implementatieplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Een werkende chatbot-widget op alle 8 blog-pagina's die Claude Opus 4.7 als AI subsidie assistent draait, met n8n + Supabase als backend, leads automatisch naar `rick@alpacaintegrations.ai` mailt met chathistorie, en als één deploy samen met de blogs live gaat.

**Architectuur:** Frontend = vanilla JS widget (geen framework) die met een n8n webhook praat. n8n orkestreert: Supabase-opslag voor chatlogs en leads, Anthropic API call met system-prompt + kennisbank in gecachte context, SMTP-email bij lead-capture. Alles in de bestaande Alpaca-stack (zelfde n8n-host als de scorecard).

**Tech Stack:** Vanilla JS + CSS (frontend), Node `node:test` (testing waar zinvol), n8n (workflow), Supabase (PostgreSQL), Anthropic API met `claude-opus-4-7`, Office 365 SMTP voor leadmelding.

---

## Bronnen en pre-conditions

**Pre-conditions** (geen task — context):
- Spec `docs/superpowers/specs/2026-05-11-chatbot-design.md` is goedgekeurd
- Sub-project 1 (blogs) is op `main` voltooid (13 commits ahead van origin)
- Branch: `main`, nog niet gepusht — beide sub-projecten gaan samen live

**Bronbestanden voor de kennisbank:**
- `c:\Users\hjemj\OneDrive - alpaca integrations\Ai\sprint subsidie\kennis chat bot\sprintsubsidie-ruwe-kennisbank.md`
- `c:\Users\hjemj\OneDrive - alpaca integrations\Ai\sprint subsidie\kennis chat bot\deep-research-report (6).md`
- De 8 blog markdown-bestanden in `content/blogs/` (gebruikt voor cross-referenties)
- **Open punt** voor Task 2: het oorspronkelijke `chatbot-overdracht-claude-code.md` document vermeldde een `alpaca-expertise-kennisbank.md` — die staat niet in de bron-folder. Bij Task 2 nemen we dit op door de blogposts zelf als Alpaca-expertise mee te compileren in de kennisbank (de blogs zíjn de geconsolideerde Alpaca expertise over subsidie + automatisering).

---

## Bestandsstructuur (eindstaat)

```
website/
├── js/
│   └── chatbot/
│       ├── widget.js         (main controller, mount on DOMContentLoaded)
│       ├── api.js            (HTTP POST naar n8n webhook)
│       ├── state.js          (localStorage session + history)
│       ├── ui.js             (DOM build: floating btn, panel, messages)
│       └── config.js         (webhook URL + welcome message)
├── css/
│   └── chatbot.css           (alle widget styling)
├── scripts/
│   ├── build-blogs.js        (uitgebreid: chatbot-assets + CTA injectie)
│   ├── templates/
│   │   ├── blog-template.html   (uitgebreid: chatbot script/css refs)
│   │   └── cta-block.html       (NIEUW: CTA-blok snippet)
│   └── tests/
│       └── state.test.js     (NIEUW: tests voor state.js)
├── docs/
│   └── chatbot/
│       ├── SETUP.md          (NIEUW: runbook voor Rick)
│       ├── supabase-migration.sql  (NIEUW)
│       ├── system-prompt.md  (NIEUW: gecompileerd uit kennisbank-bronnen)
│       ├── n8n-workflow.json (NIEUW: importklare workflow)
│       └── email-template.html (NIEUW: leadmelding template)
└── blog/*.html               (regenereerd met chatbot-assets ingebouwd)
```

---

## Task 1: Supabase migration SQL

**Files:**
- Create: `docs/chatbot/supabase-migration.sql`

Schrijf de SQL voor 3 tabellen die de chatbot-data opslaan. Rick draait dit eenmalig in de Supabase SQL editor.

- [ ] **Step 1: Maak het migration-bestand**

`docs/chatbot/supabase-migration.sql`:
```sql
-- Chatbot widget data: sessions, messages, leads
-- Voer eenmalig uit in Supabase SQL editor

CREATE TABLE IF NOT EXISTS chatbot_sessions (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  started_at timestamptz NOT NULL DEFAULT now(),
  blog_slug text,
  user_agent text
);

CREATE TABLE IF NOT EXISTS chatbot_messages (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id uuid NOT NULL REFERENCES chatbot_sessions(id) ON DELETE CASCADE,
  role text NOT NULL CHECK (role IN ('user', 'assistant')),
  content text NOT NULL,
  tool_use_json jsonb,
  created_at timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS chatbot_leads (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id uuid REFERENCES chatbot_sessions(id),
  naam text NOT NULL,
  contact_voorkeur text NOT NULL CHECK (contact_voorkeur IN ('email', 'telefoon')),
  email text,
  telefoon text,
  beste_moment text,
  context text,
  created_at timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_messages_session ON chatbot_messages(session_id, created_at);
CREATE INDEX IF NOT EXISTS idx_leads_created ON chatbot_leads(created_at DESC);

-- RLS uitgeschakeld voor service role; n8n draait met service role key
ALTER TABLE chatbot_sessions ENABLE ROW LEVEL SECURITY;
ALTER TABLE chatbot_messages ENABLE ROW LEVEL SECURITY;
ALTER TABLE chatbot_leads   ENABLE ROW LEVEL SECURITY;

-- Service role bypasst RLS automatisch, geen public policies nodig
```

- [ ] **Step 2: Commit**

```bash
git add docs/chatbot/supabase-migration.sql
git commit -m "chatbot: supabase migration sql (3 tabellen)"
```

---

## Task 2: System prompt compileren

**Files:**
- Create: `docs/chatbot/system-prompt.md`

Compileer de complete system prompt uit drie bronnen:
1. De persona/regels (uit het `chatbot-overdracht-claude-code.md` document, met de drie tone-aanpassingen)
2. Subsidie-kennisbank (uit `sprintsubsidie-ruwe-kennisbank.md` en `deep-research-report (6).md`)
3. Alpaca-expertise (uit de 8 blog-markdown bestanden in `content/blogs/`)

Het resultaat is één markdown-bestand dat 1-op-1 als `system` parameter naar de Anthropic API gestuurd wordt.

- [ ] **Step 1: Bronbestanden inventariseren**

Run vanuit website root:
```bash
ls "c:/Users/hjemj/OneDrive - alpaca integrations/Ai/sprint subsidie/kennis chat bot/"
ls content/blogs/
```
Verwacht: zie kennisbank-bestanden + 8 blog-markdown bestanden.

- [ ] **Step 2: Schrijf de persona/regels sectie**

`docs/chatbot/system-prompt.md` begint met:
```markdown
# AI subsidie assistent — system prompt

Je bent de AI subsidie assistent van Alpaca Integrations. Je helpt MKB-ondernemers met vragen over de Sprintsubsidie MKB 2026-2027 van de Provincie Limburg en over automatiseren van taken en processen.

## Wie je bent

Je bent informeel, behulpzaam en eerlijk. Je schrijft in normale taal, geen subsidietaal en geen corporate-gelul. Als iets niet past bij de regeling, zeg je dat ook. Je bent geen cheerleader en je doet niet aan overselling. Je tone is proactief: niet "ik denk met je mee" maar "dan ga ik het voor je regelen".

Je hebt twee expertisegebieden:
1. De Sprintsubsidie: regels, voorwaarden, bedragen, deadlines, aanvraagproces
2. Automatiseren van taken en processen: welke processen kun je automatiseren, wanneer AI vs. vaste logica, welke projecten passen bij de subsidie

## Welkomstmessage

Bij het openen van het chatvenster (voordat de gebruiker iets typt):
"Hoi! Ik ben de AI subsidie assistent. Ik weet alles over de Sprintsubsidie MKB en het automatiseren van taken en processen. Vertel wat je wilt weten of beschrijf je situatie — dan ga ik het voor je regelen."

[... volledige persona-regels uit het overdrachtsdocument, met "subsidie-assistent" → "AI subsidie assistent" en "ik denk mee" → "ik ga het voor je regelen" tone-aanpassingen ...]
```

Vul de volledige persona/regels in op basis van het `chatbot-overdracht-claude-code.md` document dat de gebruiker eerder deelde — secties "Wie je bent", "Kernfeiten", "Hoe je gesprekken voert", "Gespreksstrategie", "Prijsvragen", "Subsidie-context", "Lead capture flow", "Doorverwijzingen", "Wat je NIET doet", "Taalgebruik en technisch niveau".

- [ ] **Step 3: Append de subsidie-kennisbank**

Voeg de complete tekst toe van:
- `c:/Users/hjemj/OneDrive - alpaca integrations/Ai/sprint subsidie/kennis chat bot/sprintsubsidie-ruwe-kennisbank.md`
- `c:/Users/hjemj/OneDrive - alpaca integrations/Ai/sprint subsidie/kennis chat bot/deep-research-report (6).md`

Plak deze onder een sectie:
```markdown
## Kennisbank: Sprintsubsidie regelgeving

[volledige inhoud van sprintsubsidie-ruwe-kennisbank.md]

## Kennisbank: Achtergrondonderzoek Sprintsubsidie

[volledige inhoud van deep-research-report (6).md]
```

- [ ] **Step 4: Append de Alpaca-expertise (uit de 8 blogs)**

Voeg een sectie toe:
```markdown
## Kennisbank: Alpaca expertise — automatiseren en AI-implementatie

De volgende artikelen zijn op alpacaintegrations.ai gepubliceerd en bevatten Alpaca's expertise over automatisering. Gebruik deze inhoud bij vragen over automatisering, AI vs. vaste logica, project-frameing voor de subsidie, en als bron voor verwijzingen.

### [blog-titel 1] (/blog/<slug>)
[volledige inhoud van content/blogs/sprintsubsidie-pillar-blog.md]

### [blog-titel 2] (/blog/<slug>)
[volledige inhoud van content/blogs/sprintsubsidie-voorwaarden-blog.md]

[... etc voor alle 8 blogs ...]
```

Dit zorgt dat de bot zowel naar de blogs kan verwijzen voor verdieping, als de inhoud direct kan citeren als antwoord op specifieke vragen.

- [ ] **Step 5: Voeg tools-instructies toe**

Aan het einde van het bestand:
```markdown
## Tools

Je hebt twee tools tot je beschikking:

### lead_capture
Roep deze tool aan wanneer de gebruiker EXPLICIET heeft aangegeven dat ze contact willen. Vraag eerst naam, contact-voorkeur, gegevens, en bevestig dan met de tool. Ná tool-call: bedank de gebruiker en bevestig dat Rick contact opneemt.

### verwijs_blog
Roep deze tool aan om de gebruiker te verwijzen naar een specifiek blog voor verdieping. Doe dit zonder verklaring vooraf — gebruik het resultaat direct in je natuurlijke antwoord.

## Belangrijk

- NOOIT specifieke tools (n8n, Supabase, OpenAI, Zapier, Make, etc.) noemen in je antwoorden
- NOOIT prijzen of indicaties geven
- NOOIT garanties geven over goedkeuring van aanvragen
- NOOIT gevoelige bedrijfsdata (KvK-nummer, financiele cijfers) opslaan via lead_capture
- Spiegel het taalniveau van de gebruiker
- Als gebruiker een specifieke software noemt en vraagt of die gekoppeld kan worden: zeg dat het meestal kan en bied aan dat Rick het beter kan beoordelen
```

- [ ] **Step 6: Verifieer token-omvang**

Run vanuit website root:
```bash
node -e "const fs = require('node:fs'); const t = fs.readFileSync('docs/chatbot/system-prompt.md', 'utf8'); console.log('Karakters:', t.length); console.log('Geschatte tokens (chars/4):', Math.round(t.length / 4));"
```
Expected: tussen 25.000 en 50.000 tokens. Als boven 80.000: te groot voor effectieve caching, contact opnemen met user voor scope-aanpassing.

- [ ] **Step 7: Commit**

```bash
git add docs/chatbot/system-prompt.md
git commit -m "chatbot: system prompt gecompileerd uit kennisbank-bronnen"
```

---

## Task 3: Email-template (HTML + plain text)

**Files:**
- Create: `docs/chatbot/email-template.html`

Een HTML-template (met plain-text fallback) die n8n gebruikt bij elke lead-capture. Variabelen in `{{handlebars}}` syntax, n8n vult ze in.

- [ ] **Step 1: Schrijf email-template**

`docs/chatbot/email-template.html`:
```html
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<style>
  body { font-family: -apple-system, Segoe UI, Roboto, sans-serif; color: #1a1a2e; max-width: 600px; margin: 0 auto; padding: 20px; }
  h1 { color: #ec4899; font-size: 20px; margin-bottom: 20px; }
  .card { background: #f9fafb; border-left: 4px solid #ec4899; padding: 16px; margin: 16px 0; border-radius: 4px; }
  .label { font-weight: 600; color: #6b7280; font-size: 13px; text-transform: uppercase; letter-spacing: 0.05em; }
  .value { font-size: 16px; margin-top: 4px; }
  .history { background: #ffffff; border: 1px solid #e5e7eb; border-radius: 4px; padding: 16px; margin-top: 16px; font-size: 14px; line-height: 1.6; }
  .history .msg-user { color: #1e40af; margin-bottom: 12px; }
  .history .msg-assistant { color: #4b5563; margin-bottom: 12px; padding-left: 12px; border-left: 2px solid #ec4899; }
  .footer { font-size: 12px; color: #6b7280; margin-top: 24px; padding-top: 16px; border-top: 1px solid #e5e7eb; }
  a { color: #ec4899; }
</style>
</head>
<body>
  <h1>Nieuwe lead via AI subsidie assistent</h1>

  <div class="card">
    <div class="label">Naam</div>
    <div class="value">{{naam}}</div>
  </div>

  <div class="card">
    <div class="label">Voorkeur contact</div>
    <div class="value">{{contact_voorkeur}}</div>
  </div>

  {{#if email}}
  <div class="card">
    <div class="label">E-mailadres</div>
    <div class="value"><a href="mailto:{{email}}">{{email}}</a></div>
  </div>
  {{/if}}

  {{#if telefoon}}
  <div class="card">
    <div class="label">Telefoon</div>
    <div class="value"><a href="tel:{{telefoon}}">{{telefoon}}</a></div>
  </div>
  {{/if}}

  {{#if beste_moment}}
  <div class="card">
    <div class="label">Beste moment voor contact</div>
    <div class="value">{{beste_moment}}</div>
  </div>
  {{/if}}

  <div class="card">
    <div class="label">Context (samenvatting door bot)</div>
    <div class="value">{{context}}</div>
  </div>

  <h2 style="color: #1a1a2e; font-size: 16px; margin-top: 24px;">Volledige chathistorie</h2>
  <div class="history">
    {{#each messages}}
      {{#if (eq role "user")}}
        <div class="msg-user"><strong>[{{timestamp}}] Bezoeker:</strong> {{content}}</div>
      {{else}}
        <div class="msg-assistant"><strong>[{{timestamp}}] AI assistent:</strong> {{content}}</div>
      {{/if}}
    {{/each}}
  </div>

  <div class="footer">
    Sessie-ID: {{session_id}}<br>
    Bekijk in Supabase: <a href="https://supabase.com/dashboard/project/_/editor/chatbot_leads?id={{lead_id}}">chatbot_leads</a><br>
    Automatisch verzonden door n8n workflow chatbot-flow.
  </div>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add docs/chatbot/email-template.html
git commit -m "chatbot: html email-template voor leadmelding"
```

---

## Task 4: n8n workflow JSON

**Files:**
- Create: `docs/chatbot/n8n-workflow.json`

Workflow met 7 nodes die de chatbot-request volledig afhandelt: webhook ontvangen, user message opslaan, Anthropic API call met system prompt + tools, lead-handling bij tool_use, assistant message opslaan, response retourneren.

- [ ] **Step 1: Schrijf de workflow JSON**

`docs/chatbot/n8n-workflow.json`:
```json
{
  "name": "chatbot-flow",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "chatbot",
        "responseMode": "responseNode",
        "options": {
          "allowedOrigins": "https://alpacaintegrations.ai,http://localhost:8000"
        }
      },
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1.1,
      "position": [240, 300],
      "id": "node-webhook"
    },
    {
      "parameters": {
        "operation": "insert",
        "table": "chatbot_messages",
        "columns": "session_id,role,content",
        "additionalFields": {}
      },
      "name": "Save user message",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [460, 300],
      "id": "node-save-user",
      "credentials": { "supabaseApi": { "name": "Supabase service role" } }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.anthropic.com/v1/messages",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            { "name": "x-api-key", "value": "={{$credentials.anthropicApi.apiKey}}" },
            { "name": "anthropic-version", "value": "2023-06-01" },
            { "name": "content-type", "value": "application/json" }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"model\": \"claude-opus-4-7\",\n  \"max_tokens\": 1024,\n  \"system\": [\n    {\n      \"type\": \"text\",\n      \"text\": $('Read system prompt').first().json.systemPrompt,\n      \"cache_control\": { \"type\": \"ephemeral\" }\n    }\n  ],\n  \"messages\": $json.history.concat([{\"role\": \"user\", \"content\": $json.user_message}]),\n  \"tools\": [\n    {\n      \"name\": \"lead_capture\",\n      \"description\": \"Sla contactgegevens op van een potentiele lead. Gebruik alleen nadat de gebruiker expliciet heeft aangegeven dat ze contact willen.\",\n      \"input_schema\": {\n        \"type\": \"object\",\n        \"properties\": {\n          \"naam\": { \"type\": \"string\" },\n          \"contact_voorkeur\": { \"type\": \"string\", \"enum\": [\"email\", \"telefoon\"] },\n          \"email\": { \"type\": \"string\" },\n          \"telefoon\": { \"type\": \"string\" },\n          \"beste_moment\": { \"type\": \"string\" },\n          \"context\": { \"type\": \"string\" }\n        },\n        \"required\": [\"naam\", \"contact_voorkeur\", \"context\"]\n      }\n    },\n    {\n      \"name\": \"verwijs_blog\",\n      \"description\": \"Verwijs de gebruiker naar een relevante blogpost voor meer informatie.\",\n      \"input_schema\": {\n        \"type\": \"object\",\n        \"properties\": {\n          \"blog\": { \"type\": \"string\", \"enum\": [\"pillar\", \"voorwaarden\", \"ai\", \"automatisering\", \"processen\", \"tafelgasten\", \"ecommerce\", \"aanvragen\"] }\n        },\n        \"required\": [\"blog\"]\n      }\n    }\n  ]\n}"
      },
      "name": "Anthropic API",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [680, 300],
      "id": "node-anthropic",
      "credentials": { "anthropicApi": { "name": "Anthropic API key" } }
    },
    {
      "parameters": {
        "filePath": "/data/chatbot/system-prompt.md",
        "options": {}
      },
      "name": "Read system prompt",
      "type": "n8n-nodes-base.readBinaryFile",
      "typeVersion": 1,
      "position": [460, 480],
      "id": "node-read-prompt"
    },
    {
      "parameters": {
        "conditions": {
          "string": [
            { "value1": "={{$json.stop_reason}}", "operation": "equal", "value2": "tool_use" }
          ]
        }
      },
      "name": "Is tool_use?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 1,
      "position": [900, 300],
      "id": "node-if-tool"
    },
    {
      "parameters": {
        "operation": "insert",
        "table": "chatbot_leads",
        "columns": "session_id,naam,contact_voorkeur,email,telefoon,beste_moment,context"
      },
      "name": "Save lead",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [1120, 200],
      "id": "node-save-lead",
      "credentials": { "supabaseApi": { "name": "Supabase service role" } }
    },
    {
      "parameters": {
        "fromEmail": "letstalk@alpacaintegrations.ai",
        "toEmail": "rick@alpacaintegrations.ai",
        "subject": "=Nieuwe lead via AI subsidie assistent: {{$json.naam}}",
        "emailFormat": "html",
        "html": "={{$('Render email template').first().json.html}}"
      },
      "name": "Send lead email",
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2.1,
      "position": [1340, 200],
      "id": "node-email",
      "credentials": { "smtp": { "name": "Office 365 SMTP" } }
    },
    {
      "parameters": {
        "operation": "insert",
        "table": "chatbot_messages",
        "columns": "session_id,role,content,tool_use_json"
      },
      "name": "Save assistant message",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [1340, 400],
      "id": "node-save-assistant",
      "credentials": { "supabaseApi": { "name": "Supabase service role" } }
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={ \"assistant_message\": $json.content[0].text, \"tool_results\": $json.tool_results || [] }"
      },
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [1560, 300],
      "id": "node-respond"
    }
  ],
  "connections": {
    "Webhook": { "main": [[{ "node": "Read system prompt", "type": "main", "index": 0 }]] },
    "Read system prompt": { "main": [[{ "node": "Save user message", "type": "main", "index": 0 }]] },
    "Save user message": { "main": [[{ "node": "Anthropic API", "type": "main", "index": 0 }]] },
    "Anthropic API": { "main": [[{ "node": "Is tool_use?", "type": "main", "index": 0 }]] },
    "Is tool_use?": {
      "main": [
        [{ "node": "Save lead", "type": "main", "index": 0 }],
        [{ "node": "Save assistant message", "type": "main", "index": 0 }]
      ]
    },
    "Save lead": { "main": [[{ "node": "Send lead email", "type": "main", "index": 0 }]] },
    "Send lead email": { "main": [[{ "node": "Save assistant message", "type": "main", "index": 0 }]] },
    "Save assistant message": { "main": [[{ "node": "Respond", "type": "main", "index": 0 }]] }
  },
  "settings": { "executionOrder": "v1" }
}
```

**Noot bij dit JSON-skelet:** dit is een werkbare structuur die Rick kan importeren. Bij de eerste import zal n8n waarschuwen voor ontbrekende credentials — die richt Rick eenmalig in via Task 9 (SETUP.md). Bij complexere n8n-versie-syntax kan iteratie nodig zijn bij Task 11 (smoke test).

- [ ] **Step 2: Commit**

```bash
git add docs/chatbot/n8n-workflow.json
git commit -m "chatbot: n8n workflow JSON (importklare 7-node flow)"
```

---

## Task 5: `chatbot.css` — alle widget styling

**Files:**
- Create: `css/chatbot.css`

Alle styling voor floating button, chat-panel, berichten, mobiele variant, CTA-blok. Gebruikt dezelfde CSS-tokens (`--pink`, `--dark-bg`, etc.) als de blog-template.

- [ ] **Step 1: Schrijf de stylesheet**

`css/chatbot.css`:
```css
:root {
  --chat-pink: #ec4899;
  --chat-pink-dark: #db2777;
  --chat-dark-bg: #0f0f23;
  --chat-darker-bg: #0a0a16;
  --chat-purple-border: rgba(168, 85, 247, 0.2);
  --chat-text-light: #e2e8f0;
  --chat-text-white: #ffffff;
  --chat-user-bg: #1e40af;
}

/* Floating button rechtsonder */
.chatbot-fab {
  position: fixed;
  bottom: 24px;
  right: 24px;
  z-index: 9000;
  background: var(--chat-pink);
  color: var(--chat-text-white);
  border: none;
  border-radius: 28px;
  padding: 14px 22px;
  font-family: inherit;
  font-size: 15px;
  font-weight: 600;
  line-height: 1.3;
  cursor: pointer;
  box-shadow: 0 6px 24px rgba(236, 72, 153, 0.45), 0 2px 6px rgba(0, 0, 0, 0.2);
  transition: transform 0.2s ease, box-shadow 0.2s ease;
  display: flex;
  align-items: center;
  gap: 8px;
  max-width: 320px;
}
.chatbot-fab:hover {
  transform: scale(1.03);
  box-shadow: 0 8px 28px rgba(236, 72, 153, 0.55), 0 4px 8px rgba(0, 0, 0, 0.25);
  background: var(--chat-pink-dark);
}
.chatbot-fab .chatbot-fab-icon { font-size: 18px; }
.chatbot-fab[hidden] { display: none !important; }

/* Mobile: compact */
@media (max-width: 600px) {
  .chatbot-fab { padding: 12px 18px; font-size: 14px; max-width: 220px; bottom: 16px; right: 16px; }
  .chatbot-fab-text-long { display: none; }
}
@media (min-width: 601px) {
  .chatbot-fab-text-short { display: none; }
}

/* Chat panel — desktop overlay */
.chatbot-panel {
  position: fixed;
  bottom: 24px;
  right: 24px;
  width: 400px;
  height: 600px;
  max-height: calc(100vh - 48px);
  z-index: 9100;
  background: var(--chat-dark-bg);
  border: 1px solid var(--chat-purple-border);
  border-radius: 12px;
  box-shadow: 0 12px 48px rgba(0, 0, 0, 0.5);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  color: var(--chat-text-light);
}
.chatbot-panel[hidden] { display: none !important; }

/* Mobile: full screen */
@media (max-width: 600px) {
  .chatbot-panel {
    bottom: 0; right: 0; left: 0; top: 0;
    width: 100%; height: 100%; max-height: 100%;
    border-radius: 0;
  }
}

/* Panel header */
.chatbot-header {
  background: var(--chat-pink);
  color: var(--chat-text-white);
  padding: 14px 18px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-weight: 600;
}
.chatbot-close {
  background: transparent;
  border: none;
  color: var(--chat-text-white);
  cursor: pointer;
  font-size: 22px;
  line-height: 1;
  padding: 4px 8px;
}

/* Messages area */
.chatbot-messages {
  flex: 1;
  overflow-y: auto;
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  scroll-behavior: smooth;
}
.chatbot-msg {
  max-width: 80%;
  padding: 10px 14px;
  border-radius: 14px;
  font-size: 14px;
  line-height: 1.5;
  word-wrap: break-word;
}
.chatbot-msg-user {
  background: var(--chat-user-bg);
  color: var(--chat-text-white);
  align-self: flex-end;
  border-bottom-right-radius: 4px;
}
.chatbot-msg-assistant {
  background: var(--chat-darker-bg);
  border-left: 3px solid var(--chat-pink);
  color: var(--chat-text-light);
  align-self: flex-start;
  border-bottom-left-radius: 4px;
}
.chatbot-msg a { color: var(--chat-pink); text-decoration: underline; }

/* Typing indicator */
.chatbot-typing {
  display: inline-block;
  padding: 10px 14px;
  background: var(--chat-darker-bg);
  border-left: 3px solid var(--chat-pink);
  border-radius: 14px;
  align-self: flex-start;
}
.chatbot-typing span {
  display: inline-block;
  width: 6px; height: 6px;
  background: var(--chat-text-light);
  border-radius: 50%;
  margin: 0 2px;
  animation: chatbot-blink 1.4s infinite both;
}
.chatbot-typing span:nth-child(2) { animation-delay: 0.2s; }
.chatbot-typing span:nth-child(3) { animation-delay: 0.4s; }
@keyframes chatbot-blink {
  0%, 80%, 100% { opacity: 0.3; }
  40% { opacity: 1; }
}

/* Input area */
.chatbot-input-wrap {
  padding: 12px;
  border-top: 1px solid var(--chat-purple-border);
  background: var(--chat-darker-bg);
  display: flex;
  gap: 8px;
  align-items: flex-end;
}
.chatbot-input {
  flex: 1;
  background: var(--chat-dark-bg);
  color: var(--chat-text-light);
  border: 1px solid var(--chat-purple-border);
  border-radius: 18px;
  padding: 10px 14px;
  font-family: inherit;
  font-size: 14px;
  resize: none;
  outline: none;
  max-height: 120px;
  line-height: 1.4;
}
.chatbot-input:focus { border-color: var(--chat-pink); }
.chatbot-send {
  background: var(--chat-pink);
  color: var(--chat-text-white);
  border: none;
  border-radius: 50%;
  width: 40px; height: 40px;
  cursor: pointer;
  font-size: 20px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}
.chatbot-send:hover { background: var(--chat-pink-dark); }
.chatbot-send:disabled { opacity: 0.5; cursor: not-allowed; }

/* Inline CTA-blok onderaan blog */
.chatbot-cta-block {
  max-width: 760px;
  margin: 3rem auto 1rem;
  padding: 1.5rem;
  background: var(--chat-darker-bg);
  border: 1px solid var(--chat-purple-border);
  border-left: 4px solid var(--chat-pink);
  border-radius: 8px;
  text-align: left;
}
.chatbot-cta-block p {
  color: var(--chat-text-light);
  margin: 0 0 1rem 0;
  font-size: 1.05rem;
  line-height: 1.5;
}
.chatbot-cta-block strong { color: var(--chat-text-white); }
.chatbot-cta-btn {
  display: inline-block;
  background: var(--chat-pink);
  color: var(--chat-text-white);
  padding: 12px 22px;
  border-radius: 8px;
  border: none;
  font-family: inherit;
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  text-decoration: none;
}
.chatbot-cta-btn:hover { background: var(--chat-pink-dark); }
```

- [ ] **Step 2: Commit**

```bash
git add css/chatbot.css
git commit -m "chatbot: complete widget + cta-block styling"
```

---

## Task 6: `state.js` met TDD

**Files:**
- Create: `js/chatbot/state.js`
- Create: `scripts/tests/chatbot-state.test.js`

Pure module voor localStorage management: session ID generen + bewaren, berichten array bijhouden, sessie-expiry na 24h.

- [ ] **Step 1: Schrijf failing tests**

`scripts/tests/chatbot-state.test.js`:
```javascript
import { test, describe, beforeEach } from 'node:test';
import assert from 'node:assert/strict';

// Mock localStorage voor Node test environment
const storage = new Map();
globalThis.localStorage = {
  getItem: (k) => storage.has(k) ? storage.get(k) : null,
  setItem: (k, v) => storage.set(k, String(v)),
  removeItem: (k) => storage.delete(k),
  clear: () => storage.clear()
};
globalThis.crypto = { randomUUID: () => '00000000-0000-0000-0000-' + Date.now().toString().padStart(12, '0') };

const { createState } = await import('../../js/chatbot/state.js');

describe('chatbot state', () => {
  beforeEach(() => storage.clear());

  test('genereert nieuwe session_id bij eerste gebruik', () => {
    const s = createState();
    const id = s.getSessionId();
    assert.match(id, /^[0-9a-f-]+$/);
  });

  test('hergebruikt session_id binnen 24h', () => {
    const s1 = createState();
    const id1 = s1.getSessionId();
    const s2 = createState();
    const id2 = s2.getSessionId();
    assert.equal(id1, id2);
  });

  test('genereert nieuwe session_id na expiry', () => {
    const s1 = createState();
    const id1 = s1.getSessionId();
    // simuleer 25 uur later
    const past = Date.now() - 25 * 60 * 60 * 1000;
    storage.set('chatbot_session_started', String(past));
    const s2 = createState();
    const id2 = s2.getSessionId();
    assert.notEqual(id1, id2);
  });

  test('start met lege messages array', () => {
    const s = createState();
    assert.deepEqual(s.getMessages(), []);
  });

  test('voegt user message toe', () => {
    const s = createState();
    s.addMessage('user', 'hallo');
    const msgs = s.getMessages();
    assert.equal(msgs.length, 1);
    assert.equal(msgs[0].role, 'user');
    assert.equal(msgs[0].content, 'hallo');
  });

  test('messages persisteren over instances', () => {
    const s1 = createState();
    s1.addMessage('user', 'eerste');
    s1.addMessage('assistant', 'antwoord');
    const s2 = createState();
    assert.equal(s2.getMessages().length, 2);
    assert.equal(s2.getMessages()[1].content, 'antwoord');
  });

  test('reset() wist sessie en berichten', () => {
    const s = createState();
    s.addMessage('user', 'x');
    s.reset();
    assert.deepEqual(s.getMessages(), []);
    assert.equal(storage.get('chatbot_session_id'), undefined);
  });

  test('addMessage bewaart timestamp', () => {
    const s = createState();
    s.addMessage('user', 'hi');
    const m = s.getMessages()[0];
    assert.ok(m.timestamp);
    assert.ok(new Date(m.timestamp).getTime() > 0);
  });
});
```

- [ ] **Step 2: Run tests om falen te bevestigen**

Run: `npm test -- "scripts/tests/chatbot-state.test.js"`
Expected: FAIL — module not found.

- [ ] **Step 3: Implementeer state.js**

`js/chatbot/state.js`:
```javascript
const SESSION_KEY = 'chatbot_session_id';
const STARTED_KEY = 'chatbot_session_started';
const MESSAGES_KEY = 'chatbot_messages';
const EXPIRY_MS = 24 * 60 * 60 * 1000;

function isExpired() {
  const started = parseInt(localStorage.getItem(STARTED_KEY) || '0', 10);
  if (!started) return true;
  return (Date.now() - started) > EXPIRY_MS;
}

export function createState() {
  if (isExpired()) {
    localStorage.removeItem(SESSION_KEY);
    localStorage.removeItem(STARTED_KEY);
    localStorage.removeItem(MESSAGES_KEY);
  }

  function getSessionId() {
    let id = localStorage.getItem(SESSION_KEY);
    if (!id) {
      id = crypto.randomUUID();
      localStorage.setItem(SESSION_KEY, id);
      localStorage.setItem(STARTED_KEY, String(Date.now()));
    }
    return id;
  }

  function getMessages() {
    const raw = localStorage.getItem(MESSAGES_KEY);
    return raw ? JSON.parse(raw) : [];
  }

  function addMessage(role, content) {
    const msgs = getMessages();
    msgs.push({ role, content, timestamp: new Date().toISOString() });
    localStorage.setItem(MESSAGES_KEY, JSON.stringify(msgs));
  }

  function reset() {
    localStorage.removeItem(SESSION_KEY);
    localStorage.removeItem(STARTED_KEY);
    localStorage.removeItem(MESSAGES_KEY);
  }

  return { getSessionId, getMessages, addMessage, reset };
}
```

- [ ] **Step 4: Run tests om slagen te bevestigen**

Run: `npm test -- "scripts/tests/chatbot-state.test.js"`
Expected: 8 passed.

- [ ] **Step 5: Verifieer dat alle eerdere tests nog steeds slagen**

Run: `npm test`
Expected: 60+ passed (52 blog tests + 8 nieuwe).

- [ ] **Step 6: Commit**

```bash
git add js/chatbot/state.js scripts/tests/chatbot-state.test.js
git commit -m "chatbot: state module met localStorage session + history (TDD)"
```

---

## Task 7: `config.js` en `api.js`

**Files:**
- Create: `js/chatbot/config.js`
- Create: `js/chatbot/api.js`

Config-module met de webhook URL en welkomstmessage. API-module praat met n8n.

- [ ] **Step 1: Maak config.js**

`js/chatbot/config.js`:
```javascript
// Bij eerste import is WEBHOOK_URL leeg — Rick vult deze na n8n activate
// in Task 11 (SETUP.md stap 4).
export const WEBHOOK_URL = '';

export const WELCOME_MESSAGE = "Hoi! Ik ben de AI subsidie assistent. Ik weet alles over de Sprintsubsidie MKB en het automatiseren van taken en processen. Vertel wat je wilt weten of beschrijf je situatie — dan ga ik het voor je regelen.";

export const FAB_TEXT_LONG = "💬 Vragen over de subsidie of je project? Vraag het de AI →";
export const FAB_TEXT_SHORT = "💬 Vraag";

export const CTA_TITLE = "Vragen over de Sprintsubsidie of jouw project?";
export const CTA_SUBTITLE = "Praat nu met de AI subsidie assistent.";
export const CTA_BUTTON = "Start gesprek";
```

- [ ] **Step 2: Maak api.js**

`js/chatbot/api.js`:
```javascript
import { WEBHOOK_URL } from './config.js';

export async function sendMessage({ sessionId, blogSlug, userMessage, history }) {
  if (!WEBHOOK_URL) {
    throw new Error('Chatbot WEBHOOK_URL niet geconfigureerd. Zie SETUP.md stap 4.');
  }

  const response = await fetch(WEBHOOK_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      session_id: sessionId,
      blog_slug: blogSlug,
      user_message: userMessage,
      history: history.map(m => ({ role: m.role, content: m.content }))
    })
  });

  if (!response.ok) {
    throw new Error(`Chatbot API error: ${response.status} ${response.statusText}`);
  }

  return response.json();
}
```

- [ ] **Step 3: Commit**

```bash
git add js/chatbot/config.js js/chatbot/api.js
git commit -m "chatbot: config en api modules"
```

---

## Task 8: `ui.js` — DOM bouwers

**Files:**
- Create: `js/chatbot/ui.js`

Functies die DOM-elementen bouwen: floating button, panel, message-bubbles, typing indicator, CTA-block. Geen tests — DOM-rendering test je tijdens smoke test.

- [ ] **Step 1: Schrijf ui.js**

`js/chatbot/ui.js`:
```javascript
import {
  FAB_TEXT_LONG, FAB_TEXT_SHORT,
  CTA_TITLE, CTA_SUBTITLE, CTA_BUTTON
} from './config.js';

export function createFab(onClick) {
  const btn = document.createElement('button');
  btn.className = 'chatbot-fab';
  btn.setAttribute('aria-label', 'Open chat met AI subsidie assistent');
  btn.innerHTML = `
    <span class="chatbot-fab-text-long">${FAB_TEXT_LONG}</span>
    <span class="chatbot-fab-text-short">${FAB_TEXT_SHORT}</span>
  `;
  btn.addEventListener('click', onClick);
  return btn;
}

export function createPanel({ onClose, onSend }) {
  const panel = document.createElement('div');
  panel.className = 'chatbot-panel';
  panel.setAttribute('hidden', '');
  panel.innerHTML = `
    <div class="chatbot-header">
      <span>AI subsidie assistent</span>
      <button class="chatbot-close" aria-label="Sluit chat">✕</button>
    </div>
    <div class="chatbot-messages" role="log" aria-live="polite"></div>
    <form class="chatbot-input-wrap">
      <textarea class="chatbot-input" rows="1" placeholder="Typ je bericht..." aria-label="Typ je bericht"></textarea>
      <button type="submit" class="chatbot-send" aria-label="Verstuur">→</button>
    </form>
  `;

  panel.querySelector('.chatbot-close').addEventListener('click', onClose);
  const form = panel.querySelector('form');
  const textarea = panel.querySelector('.chatbot-input');
  form.addEventListener('submit', (e) => {
    e.preventDefault();
    const text = textarea.value.trim();
    if (text) {
      onSend(text);
      textarea.value = '';
    }
  });
  textarea.addEventListener('keydown', (e) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      form.requestSubmit();
    }
  });

  return panel;
}

export function appendMessage(panel, role, content) {
  const messages = panel.querySelector('.chatbot-messages');
  const div = document.createElement('div');
  div.className = `chatbot-msg chatbot-msg-${role}`;
  // Simpele markdown-link rendering: [tekst](url) → <a>
  const html = content.replace(/\[([^\]]+)\]\(([^)]+)\)/g, (full, txt, url) => {
    const isInternal = url.startsWith('/') || url.startsWith('#');
    const attrs = isInternal ? '' : ' target="_blank" rel="noopener"';
    return `<a href="${url}"${attrs}>${txt}</a>`;
  });
  div.innerHTML = html.replace(/\n/g, '<br>');
  messages.appendChild(div);
  messages.scrollTop = messages.scrollHeight;
}

export function showTyping(panel) {
  const messages = panel.querySelector('.chatbot-messages');
  const t = document.createElement('div');
  t.className = 'chatbot-typing';
  t.setAttribute('data-typing-indicator', '');
  t.innerHTML = '<span></span><span></span><span></span>';
  messages.appendChild(t);
  messages.scrollTop = messages.scrollHeight;
}

export function hideTyping(panel) {
  const t = panel.querySelector('[data-typing-indicator]');
  if (t) t.remove();
}

export function showPanel(panel) { panel.removeAttribute('hidden'); }
export function hidePanel(panel) { panel.setAttribute('hidden', ''); }

export function buildCtaBlock(onClick) {
  const block = document.createElement('div');
  block.className = 'chatbot-cta-block';
  block.innerHTML = `
    <p><strong>${CTA_TITLE}</strong><br>${CTA_SUBTITLE}</p>
    <button class="chatbot-cta-btn" type="button">${CTA_BUTTON}</button>
  `;
  block.querySelector('.chatbot-cta-btn').addEventListener('click', onClick);
  return block;
}
```

- [ ] **Step 2: Commit**

```bash
git add js/chatbot/ui.js
git commit -m "chatbot: ui-module met fab, panel, messages, cta-block"
```

---

## Task 9: `widget.js` — main controller

**Files:**
- Create: `js/chatbot/widget.js`

Orchestreert state + api + ui. Mount op DOMContentLoaded. Initialiseert floating button + verborgen panel + wire-up.

- [ ] **Step 1: Schrijf widget.js**

`js/chatbot/widget.js`:
```javascript
import { createState } from './state.js';
import { sendMessage } from './api.js';
import {
  createFab, createPanel, appendMessage, showTyping, hideTyping,
  showPanel, hidePanel, buildCtaBlock
} from './ui.js';
import { WELCOME_MESSAGE } from './config.js';

function getBlogSlug() {
  const m = window.location.pathname.match(/\/blog\/([^/.]+)/);
  return m ? m[1] : null;
}

function init() {
  const state = createState();
  const slug = getBlogSlug();

  let fab, panel;

  function openPanel() {
    if (fab) fab.setAttribute('hidden', '');
    showPanel(panel);

    // Render bestaande messages (bij terugkomst in dezelfde sessie)
    const existing = state.getMessages();
    if (existing.length === 0) {
      appendMessage(panel, 'assistant', WELCOME_MESSAGE);
    } else {
      existing.forEach(m => appendMessage(panel, m.role, m.content));
    }
    panel.querySelector('.chatbot-input').focus();
  }

  function closePanel() {
    hidePanel(panel);
    if (fab) fab.removeAttribute('hidden');
  }

  async function handleSend(text) {
    appendMessage(panel, 'user', text);
    state.addMessage('user', text);
    showTyping(panel);

    try {
      const result = await sendMessage({
        sessionId: state.getSessionId(),
        blogSlug: slug,
        userMessage: text,
        history: state.getMessages().slice(0, -1)  // exclude de zojuist toegevoegde
      });
      hideTyping(panel);
      appendMessage(panel, 'assistant', result.assistant_message);
      state.addMessage('assistant', result.assistant_message);
    } catch (err) {
      hideTyping(panel);
      appendMessage(panel, 'assistant',
        'Sorry, er ging iets mis. Probeer het zo nog eens — of stuur een mailtje naar [letstalk@alpacaintegrations.ai](mailto:letstalk@alpacaintegrations.ai).'
      );
      console.error('Chatbot error:', err);
    }
  }

  fab = createFab(openPanel);
  panel = createPanel({ onClose: closePanel, onSend: handleSend });

  document.body.appendChild(fab);
  document.body.appendChild(panel);

  // CTA-blok — als er een <div data-chatbot-cta-mount> in de pagina staat, zet daar de CTA neer
  document.querySelectorAll('[data-chatbot-cta-mount]').forEach(mount => {
    mount.replaceWith(buildCtaBlock(openPanel));
  });
}

if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', init);
} else {
  init();
}
```

- [ ] **Step 2: Commit**

```bash
git add js/chatbot/widget.js
git commit -m "chatbot: widget controller orchestreert state/api/ui"
```

---

## Task 10: CTA-blok template + build-script uitbreiding

**Files:**
- Create: `scripts/templates/cta-block.html`
- Modify: `scripts/build-blogs.js`
- Modify: `scripts/templates/blog-template.html`

Het CTA-blok wordt onderaan elke blog ingevoegd door het build-script (voor de author-bio). Het bevat één `<div data-chatbot-cta-mount>` placeholder die de widget bij laden vervangt door het echte CTA-blok DOM.

Ook moet de blog-template `<script type="module" src="/js/chatbot/widget.js" defer></script>` en `<link rel="stylesheet" href="/css/chatbot.css">` opnemen.

- [ ] **Step 1: Maak cta-block snippet**

`scripts/templates/cta-block.html`:
```html
<div data-chatbot-cta-mount></div>
```

(Mini-snippet: de widget vervangt deze div bij laden door het volledige CTA-blok via `buildCtaBlock()`. Voordeel: we hoeven geen tekst te dupliceren in HTML én JS — staat één keer in `config.js`.)

- [ ] **Step 2: Update blog-template.html — voeg chatbot assets toe**

In `scripts/templates/blog-template.html`, vóór `</head>` (na schema markup blok):
```html
    <link rel="stylesheet" href="/css/chatbot.css">
    <script type="module" src="/js/chatbot/widget.js" defer></script>
```

- [ ] **Step 3: Update build-blogs.js — CTA-blok injectie**

Voeg een nieuwe stap toe in `buildBlog()`, NA `html = html.replace(/<h1[^>]*>...` en VÓÓR `const schemaBlocks = ...`:
```javascript
  // Voeg CTA-blok mount-point toe aan het einde van de blog body
  html = html + '\n<div data-chatbot-cta-mount></div>\n';
```

Dit zorgt dat elke blog een mount-point heeft waar de chatbot widget z'n CTA-blok inplugt.

- [ ] **Step 4: Run build en verifieer output**

Run: `npm run build`
Expected: alle 8 blogs gebouwd zonder errors.

Verifieer in één output:
```bash
grep -c "chatbot.css" blog/sprintsubsidie-mkb-limburg-gids.html
grep -c "data-chatbot-cta-mount" blog/sprintsubsidie-mkb-limburg-gids.html
```
Expected: 1 en 1.

- [ ] **Step 5: Commit**

```bash
git add scripts/templates/cta-block.html scripts/templates/blog-template.html scripts/build-blogs.js blog/*.html
git commit -m "chatbot: cta-blok injectie + widget assets in alle blogs"
```

---

## Task 11: SETUP.md runbook voor Rick

**Files:**
- Create: `docs/chatbot/SETUP.md`

Stap-voor-stap document voor Rick om alles eenmalig in te richten. Veronderstelt dat hij toegang heeft tot zijn Supabase project, n8n instance, en Anthropic console.

- [ ] **Step 1: Schrijf SETUP.md**

`docs/chatbot/SETUP.md`:
```markdown
# Chatbot setup — runbook voor Rick

Eenmalige inrichting. Hierna draait de chatbot live op alle 8 blog-pagina's.

## Vooraf

Je hebt nodig:
- Toegang tot je Supabase project (de bestaande waar de scorecard ook in zit)
- Toegang tot je n8n instance (`alpacaintegrations.n8p1.hostingsecure.com`)
- Een Anthropic API key (zie stap 2 als je die nog niet hebt)
- SMTP-toegang voor `letstalk@alpacaintegrations.ai` (Office 365)

## Stap 1: Supabase tabellen aanmaken

1. Open je Supabase dashboard → SQL Editor → New query
2. Open het bestand `docs/chatbot/supabase-migration.sql` in dit project
3. Kopieer de volledige inhoud, plak in de SQL editor, klik **Run**
4. Verifieer: ga naar Table Editor, je ziet nu drie nieuwe tabellen: `chatbot_sessions`, `chatbot_messages`, `chatbot_leads`

## Stap 2: Anthropic API key

Als je nog geen API key hebt:
1. Ga naar [console.anthropic.com](https://console.anthropic.com)
2. Maak een account / log in
3. Settings → API Keys → Create Key, geef het een naam zoals "chatbot-prod"
4. Kopieer de key meteen (je ziet hem maar één keer)
5. Bewaar 'm veilig — straks zet je hem in n8n

## Stap 3: System prompt naar n8n server

Het bestand `docs/chatbot/system-prompt.md` moet op de n8n server staan op pad `/data/chatbot/system-prompt.md` (zo verwijst de workflow ernaar).

Opties:
- **A. Via n8n filesystem-mount** als je n8n bij hostingsecure.com via Docker draait: kopieer het bestand naar de mounted folder
- **B. Via n8n Code-node** (alternatief): kopieer de volledige inhoud in een Set-node binnen de workflow. Iets minder elegant maar werkt zonder filesystem-toegang

(Bij twijfel: kies optie B. We passen de workflow daarvoor aan in stap 5 — vraag het me en ik genereer een alternatieve variant.)

## Stap 4: n8n workflow importeren

1. Open je n8n instance
2. Workflows → **Add workflow** → **Import from File**
3. Kies `docs/chatbot/n8n-workflow.json` uit dit project
4. De workflow verschijnt met 7 nodes en gebroken credentials-icoontjes (rood)

## Stap 5: Credentials in n8n inrichten

Drie credentials nodig. Per node klik je op het rode credentials-icoontje en richt je hem in:

### Anthropic API key
- Type: HTTP Header Auth (of de built-in Anthropic credential als je n8n versie die heeft)
- Header naam: `x-api-key`
- Header value: jouw Anthropic API key uit stap 2

### Supabase service role
- Type: Supabase
- URL: jouw Supabase project URL (begin met `https://`)
- Service Role Key: vind je in Supabase Settings → API → service_role key (NIET de anon key)

### SMTP (voor leadmelding)
- Type: SMTP
- Host: `smtp.office365.com`
- Port: 587
- User: `letstalk@alpacaintegrations.ai`
- Password: app-wachtwoord (genereer via Microsoft 365 Account → Security → App passwords)
- Secure: STARTTLS

## Stap 6: Activeer de workflow

1. Klik bovenin op de **Active**-toggle (van grijs naar groen)
2. n8n geeft je nu een productie-webhook URL — kopieer deze, ziet er ongeveer zo uit:
   `https://alpacaintegrations.n8p1.hostingsecure.com/webhook/abc123-def456-...`

## Stap 7: Webhook URL doorgeven aan Claude Code

Geef de webhook URL door (bijv. door 'm in een chat te plakken).

Claude Code zet de URL in `js/chatbot/config.js`, runt `npm run build` opnieuw, en commit.

## Stap 8: Lokale test

1. Start de lokale server (in een terminal):
   ```
   npx http-server -p 8000 -c-1
   ```
2. Open `http://localhost:8000/blog/sprintsubsidie-mkb-limburg-gids.html`
3. Klik rechtsonder op de roze chat-button
4. Stel een testvraag, bijv. "Wat is het minimumbedrag voor de Sprintsubsidie?"
5. Verifieer: bot antwoordt binnen 5-10 seconden met correct bedrag
6. Test lead-capture: typ "ik wil dat iemand contact opneemt"
7. Volg de flow — bot vraagt om gegevens, je vult ze in
8. Verifieer:
   - Supabase Table Editor → `chatbot_leads` toont jouw test-lead
   - E-mail komt aan op `rick@alpacaintegrations.ai` met chathistorie

## Stap 9: Live zetten

Beide sub-projecten (blogs + chatbot) zijn nu klaar.

Run:
```bash
git push origin main
```

Netlify deployt automatisch. Na ±2 minuten staat alles live op alpacaintegrations.ai.

## Troubleshooting

**"Sorry, er ging iets mis"** in de chat → check de n8n execution logs voor de fout (klik op de workflow → Executions tab).

**Chatbot reageert niet** → check of de webhook URL in `js/chatbot/config.js` correct staat én of de workflow geactiveerd is in n8n.

**E-mail komt niet aan** → check de SMTP credentials. Vaakste fout: gewoon wachtwoord ipv app-wachtwoord, of poort/secure verkeerd ingesteld.

**Bot geeft verkeerde info** → de system prompt staat in `/data/chatbot/system-prompt.md` op de n8n server (of in de Set-node als je optie B koos). Pas die aan en herstart de workflow.
```

- [ ] **Step 2: Commit**

```bash
git add docs/chatbot/SETUP.md
git commit -m "chatbot: setup runbook voor Rick"
```

---

## Task 12: Smoke test infrastructuur (mock-mode)

**Files:**
- Modify: `js/chatbot/config.js`
- Create: `js/chatbot/mock.js`

Zorg dat de widget lokaal te testen is zonder werkende n8n-workflow — handig voor pre-launch UI-verificatie. Een eenvoudige mock-flag in config schakelt mock responses in.

- [ ] **Step 1: Maak mock.js**

`js/chatbot/mock.js`:
```javascript
// Mock voor lokale UI-tests zonder n8n. Zet ENABLE_MOCK=true in config.js.

const FAKE_RESPONSES = [
  "Goeie vraag! De Sprintsubsidie is minimaal 5.000 en maximaal 24.500 euro, met 50% van je projectkosten.",
  "Voor MKB-bedrijven in Limburg. Niet voor zzp'ers of agrarische bedrijven met SBI-code A.",
  "De eerstvolgende tranche loopt van 11 mei tot 11 juni 2026.",
  "Dat klinkt als een typisch automatiseringsproject. Vertel iets meer over hoe het nu loopt — handmatig in Excel? Of via een ander systeem?"
];

let callIdx = 0;

export async function mockSendMessage({ userMessage }) {
  await new Promise(r => setTimeout(r, 800 + Math.random() * 1200));
  const reply = FAKE_RESPONSES[callIdx % FAKE_RESPONSES.length];
  callIdx++;
  return { assistant_message: reply, tool_results: [] };
}
```

- [ ] **Step 2: Update config.js + api.js voor mock-mode**

In `js/chatbot/config.js`, voeg toe:
```javascript
export const ENABLE_MOCK = false;  // zet op true voor lokale UI-tests zonder n8n
```

In `js/chatbot/api.js`, update:
```javascript
import { WEBHOOK_URL, ENABLE_MOCK } from './config.js';
import { mockSendMessage } from './mock.js';

export async function sendMessage(opts) {
  if (ENABLE_MOCK) return mockSendMessage(opts);

  if (!WEBHOOK_URL) {
    throw new Error('Chatbot WEBHOOK_URL niet geconfigureerd. Zie SETUP.md stap 4.');
  }

  const response = await fetch(WEBHOOK_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      session_id: opts.sessionId,
      blog_slug: opts.blogSlug,
      user_message: opts.userMessage,
      history: opts.history.map(m => ({ role: m.role, content: m.content }))
    })
  });

  if (!response.ok) {
    throw new Error(`Chatbot API error: ${response.status} ${response.statusText}`);
  }

  return response.json();
}
```

- [ ] **Step 3: Run build en doe een lokale UI-test**

```bash
# Zet ENABLE_MOCK = true in config.js
npm run build
npx http-server -p 8000 -c-1
```

Open `http://localhost:8000/blog/sprintsubsidie-mkb-limburg-gids.html` en test:
- [ ] Roze floating button verschijnt rechtsonder
- [ ] Klik → chat-panel opent, welkomstmessage zichtbaar
- [ ] Type een bericht, druk Enter → mock-antwoord verschijnt
- [ ] Refresh de pagina → klik FAB opnieuw → vorige messages staan er nog
- [ ] CTA-blok onderaan blog → klik → opent het panel
- [ ] Sluit panel → FAB komt terug
- [ ] Test mobiel (resize browser tot < 600px) → panel wordt full-screen, FAB compact

- [ ] **Step 4: Zet ENABLE_MOCK terug op false**

In `js/chatbot/config.js`:
```javascript
export const ENABLE_MOCK = false;
```

- [ ] **Step 5: Commit**

```bash
git add js/chatbot/mock.js js/chatbot/config.js js/chatbot/api.js
git commit -m "chatbot: mock-mode voor lokale UI-tests zonder n8n"
```

---

## Task 13: Eindreview en pre-launch checklist

**Files:** geen wijzigingen — verificatie-taak

- [ ] **Step 1: Run alle tests**

```bash
npm test
```
Expected: 60+ passed (52 blog + 8 chatbot-state).

- [ ] **Step 2: Run build**

```bash
npm run build
```
Expected: 8 blogs + sitemap zonder errors.

- [ ] **Step 3: Verifieer dat alle chatbot-files bestaan**

```bash
ls js/chatbot/
ls css/chatbot.css
ls docs/chatbot/
```
Expected:
- `js/chatbot/` bevat: `widget.js`, `api.js`, `state.js`, `ui.js`, `config.js`, `mock.js`
- `css/chatbot.css` bestaat
- `docs/chatbot/` bevat: `SETUP.md`, `supabase-migration.sql`, `system-prompt.md`, `n8n-workflow.json`, `email-template.html`

- [ ] **Step 4: Verifieer dat elke blog de chatbot-assets bevat**

```bash
for f in blog/*.html; do
  echo "=== $f ==="
  grep -c "chatbot.css" "$f"
  grep -c "widget.js" "$f"
  grep -c "data-chatbot-cta-mount" "$f"
done
```
Expected: per blog 3x 1 (één CSS-link, één script-tag, één CTA mount-point).

- [ ] **Step 5: Verifieer git-status**

```bash
git status
git log --oneline origin/main..HEAD | head -30
```
Expected: een schone working tree, ~25 commits ahead van origin (de 13 blog-commits + 12 chatbot-commits).

- [ ] **Step 6: Rapporteer "klaar voor Rick's setup"**

Beide sub-projecten zijn nu klaar. Verdere actie ligt bij Rick:
1. Run `docs/chatbot/SETUP.md` stap voor stap
2. Webhook URL doorgeven aan Claude Code → `config.js` update
3. Eind-testronde lokaal (echte n8n, niet mock)
4. `git push origin main` → live deploy

---

## Self-review samenvatting

**Spec coverage:**
- Architectuur (webhook → n8n → Anthropic → Supabase → SMTP): ✅ Tasks 1, 4
- Supabase schema: ✅ Task 1
- System prompt + kennisbank: ✅ Task 2
- n8n workflow JSON: ✅ Task 4
- Email-template: ✅ Task 3
- Frontend modules (state, api, ui, widget, config): ✅ Tasks 6, 7, 8, 9
- Mock-mode voor lokale tests: ✅ Task 12
- Chatbot styling (floating btn, panel, cta-block): ✅ Task 5
- CTA-blok injectie in blogs: ✅ Task 10
- SETUP runbook voor Rick: ✅ Task 11
- Pre-launch checklist: ✅ Task 13

**Placeholder scan:** Geen "TBD" of "implement later" in concrete steps. Eén bewust open punt: het pad waar `system-prompt.md` op de n8n-server staat (filesystem vs Set-node). Behandeld in SETUP.md stap 3 als keuze die Rick samen met mij maakt afhankelijk van zijn n8n setup.

**Type consistency:** Module-exports zijn consistent (`createState`, `sendMessage`, `createFab`, `createPanel`, `appendMessage`, etc.) tussen definitie en gebruik. `WEBHOOK_URL`, `WELCOME_MESSAGE`, `ENABLE_MOCK` consistent in config.js en consumers.

**Scope check:** Plan beperkt zich tot sub-project 2 (chatbot). Geen wijzigingen aan de bestaande blog-pipeline behalve de CTA-mount toevoeging in Task 10. Werkverdeling tussen mij (genereren) en Rick (extern configureren) staat expliciet in Task 11 (SETUP.md).
